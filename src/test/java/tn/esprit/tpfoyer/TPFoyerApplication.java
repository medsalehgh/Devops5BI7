package tn.esprit.tpfoyer;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.assertTrue;
import org.junit.jupiter.api.Test;
@SpringBootTest
public class TPFoyerApplication {
    @Test
    void contextLoads() {
        // This assertion checks that the application context loads correctly.
        // Replace the assertTrue(true) with actual assertions related to your application if needed.
        assertTrue(true); // This is a placeholder. You can add specific checks if required.
    }
}
